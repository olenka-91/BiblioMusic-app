package handler

import (
	"github.com/gin-gonic/gin"
)

func (h *Handler) createSong(ctx *gin.Context) {
	// groupName := ctx.Param("groupName")
	// songName := ctx.Param("songName")

	// if err := ctx.BindJSON(&input); err != nil {
	// 	logrus.Error(err.Error())
	// }

	// id, err := h.services.Song.Create(userID, input)
	// if err != nil {
	// 	newErrorResponce(ctx, http.StatusInternalServerError, err.Error())
	// }

	// ctx.JSON(http.StatusOK, map[string]interface{}{
	// 	"GroupId": groupId,
	// 	"SongId":  songId,
	// })

}
